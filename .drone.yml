pipeline:
    flake:
        image: python:3.5.3
        pull: true
        commands:
            - pip install flake8 pyinstaller
            - pip install -r requirements.txt
            - flake8
            - pyinstaller -F -n subnetter-linux-x64 subnetter.py
    github_release:
        image: plugins/github-release
        files: dist/subnetter-linux-x64
        when:
            event: tag
