# {{ name }}, port: {{ port }}
subnet {{ network }} netmask {{ netmask }}  {
    option subnet-mask {{ netmask }} ;
    option routers {{ gateway }};
    option domain-name "{{ name|lower }}.somelan";
    ddns-domainname "{{ name|lower }}.somelan";
    range {{ start }} {{ end }};
}
